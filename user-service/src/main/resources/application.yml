server:
  port: 8081

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5432/barber_booking
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
